spring:
  datasource:
    username: root
    password: Rf^uvM^k
    url: jdbc:mysql://localhost:3306/db_ecommerce?serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties.hibernate.dialect: org.hibernate.dialect.MySQL5Dialect
server:
  port: 9191
#app
backendweb:
  app:
    jwtSecret: backendSecretKey
    jwtExpirationMs: 86400000